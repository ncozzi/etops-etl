transaction_data_project:
  target: dev

  outputs:
    dev:
      type: sqlite
      threads: 1
      database: transactions
      schema: main
      schemas_and_paths:
        main: /opt/airflow/data/transactions.db
      schema_directory: /opt/airflow/data